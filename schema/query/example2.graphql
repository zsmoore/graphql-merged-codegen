query example($id: ID!) {
    Tweet(id: $id) {
        id
        body
        date
        Author {
            first_name
        }
        Stats {
            views
            likes
            retweets
            user {
                first_name
            }
            test
        }
    }
}